void main(){
  // this is possible
  // final API_KEY = fetchApi();
  // but this is impossible
  const API_KEY = fetchApi();

  // it can use define variable especially
  const max_allowed_participate = 120;
}